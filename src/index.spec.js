import { expect } from 'chai'

describe('batchProcessor', function () {
  it('should accept a url and a destination', function () {})

  it('should retrieve works as json from the api', function () {})
})
